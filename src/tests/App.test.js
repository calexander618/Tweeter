import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('The app renders without crashing', (done) => {
  // Step 1: Mount the react component to a DOM element using ReactDOM.render()
  // Step 2: Check that everything renders correctly using expect()
  // Step 3: If successful, unmount the component to reset for the next test.
  done();
});

// Continue test descriptions here. Write as many as you feel necessary.

it('A user is added to the data table', (done) => {
  // describe test steps here
  // Step 1: Mount the react component to a DOM element using ReactDOM.render()
  // Step 2: Get the current searchResults array data from the state and add the user data
  // Step 3: Update the component's state with the updated data array using setState()
  // Step 4: Check that everything renders correctly using expect()
  // Step 5: If successful, unmount the component to reset for the next test.
  done();
});

it('A user is deleted from the data table', (done) => {
  // describe test steps here
  // Step 1: Mount the react component to a DOM element using ReactDOM.render()
  // Step 2: Get the current searchResults array data from the state and remove the user data
  // Step 3: Update the component's state with the updated data array using setState()
  // Step 4: Check that everything renders correctly using expect()
  // Step 5: If successful, unmount the component to reset for the next test.
  done();
});

it('SearchResult component displays data correctly', (done) => {
  // describe test steps here
  // Step 1: Mount the react component to a DOM element using ReactDOM.render()
  // Step 2: Pass the SearchResult component the desired user data as a prop
  // Step 2: Check that everything renders correctly using expect()
  // Step 3: If successful, unmount the component to reset for the next test.
  done();
});

